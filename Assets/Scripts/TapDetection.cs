using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TapDetection : MonoBehaviour
{
    private InputManager inputManager;
    /*

    private void Awake()
    {
        inputManager = InputManager.Instance;

    }
    private void OnEnable()
    {
        inputManager.OnStartTouch += TapStart;
        inputManager.OnStartTouch += TapEnd;
    }
    private void Start(Vector2 position, float time)
    {
        
    }
    private void OnDisable()
    {
        inputManager.OnStartTouch -= TapStart;
        inputManager.OnStartTouch -= TapEnd;
    }
    
    public void TapStart(Vector2 screenPosition, float time)
    {
        
        //Vector3 screenCoordinates = new Vector3()
    }
    public void TapEnd(Vector2 screenPosition, float time)
    {

    }*/
}
